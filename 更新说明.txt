================================================================================
                        远程桌面管理工具 - 更新说明
================================================================================

版本：2.2.20260825
更新日期：2026-08-25

--------------------------------------------------------------------------------
【本次更新内容】
--------------------------------------------------------------------------------

1. RDP 连接重构 —— 临时 .rdp 配置文件 + Windows 凭据管理器
   - 连接前先清理凭据管理器中与本次主机相关的旧凭据（TERMSRV/ip），
     避免残留凭据造成密码冲突
   - 将用户名、密码写入 Windows 凭据管理器（cmdkey，列表形式传参，
     密码含 &、|、<、> 等特殊字符时安全）
   - 根据远程桌面高级选项生成临时 .rdp 配置文件：tempfile.mkstemp
     生成唯一文件名，多实例并发连接互不干扰；文件中不含密码，仅含
     连接地址（full address）、用户名与高级选项配置
   - 执行 mstsc 打开该 .rdp 启动连接，高级选项随连接自动生效
   - 远程桌面会话关闭后自动清理：删除凭据 + 删除临时 .rdp 文件

2. 远程桌面高级选项（新增，持久化存储）
   - 支持远程音频位置（本地/远程）、启用剪贴板、映射本地驱动器、
     全屏模式、远程桌面分辨率（多档预设 + 自定义宽x高）
   - 选项修改即时保存到 SQLite（settings 表），程序重启后自动恢复
   - RDP 连接时自动读取并应用这些选项（写入临时 .rdp 文件）

3. Ping 批量检测（新增）
   - 一键遍历服务器树中全部主机，逐台执行 ping 检测
   - 检测期间弹出进度窗口，实时显示"当前/总数"与正在检测的主机
   - 检测完成后在服务器树"状态"列更新图标：网络正常显示绿色 ✓，
     异常显示红色 ✗
   - 后台线程 + 队列轮询刷新界面，检测期间界面不卡死；按钮防重复
     点击（检测中置灰）

4. 主界面新增下方功能区（左右布局）
   - 服务器树下方新增左右布局容器：左侧为服务器说明（沿用既有
     组件），右侧为功能区
   - 右侧功能区为上下结构：上方为 Ping 检测按钮与 FAQ 知识库按钮
     （左右排列、两侧与中间边距均衡），下方为远程桌面高级选项
     配置区
   - 远程桌面高级选项区域占满剩余高度，内容一屏展示、无需滚动

5. 服务器树密码列脱敏
   - 密码列默认显示"*********"，不再直接展示明文密码
   - 选中行后右键菜单点击"显示密码"，即可在当前行查看明文密码

6. 服务器树列宽优化
   - 图标列（第一列）宽度缩小，IP地址列宽度增加

7. FAQ 知识库按钮（占位）
   - 右侧功能区上方放置 FAQ 知识库按钮（img/btn-faqzsk.png，
     191x45）
   - 点击提示"FAQ知识库功能正在开发中"，具体逻辑后续版本实现

8. 界面细节优化
   - 顶部按钮区（含搜索区）固定背景色 #F0F0F0，不随界面背景设置
     变化，避免按钮图片突兀
   - "主机名或域名搜索"、"服务器说明"两个标签背景不跟随界面背景色
     变化
   - Ping/FAQ 按钮左右外边距（各 25px）、按钮间距与上边距加大，
     布局更舒展

9. 重命名分组（已实现）
   - 右键分组树中的分组，点击"重命名分组"即可修改分组名称
   - 重命名结果即时持久化到数据库，无需重启

10. 编辑主机支持修改"主机类型"（已实现）
   - 编辑主机的字段下拉框新增"主机类型"选项
   - 选择后弹出类型选择对话框，可在 SSH / RDP / VNC / Radmin / URL
     之间切换，修改即时保存到数据库

11. 窗口位置与大小记忆（已实现）
   - 程序关闭前自动保存主窗口的位置与尺寸到 settings 表
     （last_window_x / y / width / height）
   - 下次启动时自动恢复上次布局，无需手动调整

--------------------------------------------------------------------------------
【历史更新记录】
--------------------------------------------------------------------------------

2026-08-25 | 版本 2.1.20260825
  - RDP 连接重构为 Windows 凭据管理器方式（cmdkey 写入/自动清除凭据，
    当时不再生成临时文件；密码特殊字符列表传参安全）
  - 编辑主机功能重构与修复（选中数据获取、主机名修改、纯数字地址
    类型错误三项修复，新增输入校验）
  - RDP/URL 补齐连接确认弹框（五种连接类型齐全）
  - 新增鼠标中键释放选中焦点
  - 修复分组展开状态多层级记录失败、空分组不刷新列表
  - 修复启动 -font 报错；界面颜色设置保存后实时生效
  - 分组树图标更换为 folder/folder_badge_plus（24x24）

2026-08-24 | 版本 2.0.20260824
  - 新增设置按钮与配置管理（连接工具路径、连接参数、界面设置）
  - 新增 SSH/VNC 一键连接功能
  - SSH 工具支持下拉选择（XTerminal/PuTTY/MobaXterm/FinalShell/Xshell）
  - 设置窗口居中显示；字体/颜色配置应用到列表
  - 新增颜色选择器与"恢复默认设置"按钮，保存后立即生效
  - 服务器树/分组树添加 Font Awesome 图标
  - 分组展开状态持久化（本版仅支持两级，2.1 版已修复为全层级）
  - 修复 17 个 Bug（B1-B17）

2025-01-24 | 版本 1.0.20250124
  - 修复 B1-B17 共 17 个 Bug（详见 ISSUES.md）
  - 新增 SSH/VNC 一键连接功能
  - 新增设置按钮与配置管理功能

2024-09-06 | 版本 1.0.20240906
  - 初始版本发布

--------------------------------------------------------------------------------
【已知问题】
--------------------------------------------------------------------------------
详见 ISSUES.md 文档。

--------------------------------------------------------------------------------
【不兼容改动】
--------------------------------------------------------------------------------
1. RDP 连接流程调整为：清理旧凭据 → 写入凭据 → 生成临时 .rdp（仅含
   高级选项配置与连接地址，不含密码）→ mstsc 执行 → 会话关闭后自动
   清理凭据与临时文件。连接体验不变，远程桌面高级选项随连接自动生效。
2. 服务器树密码列默认脱敏显示（*********），查看明文需选中行后右键
   "显示密码"。

================================================================================
