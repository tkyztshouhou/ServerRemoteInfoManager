# ServerRemoteInfoManager 依赖配置
# ============================================
# Python 版本：3.10.11（从 3.8.5 升级）
# 操作系统：Windows 10 / 11 64 位
#
# 本项目 GUI / 数据库 / 多线程主要使用 Python 标准库：
#   tkinter      (GUI)
#   sqlite3      (数据库)
#   os / re      (系统与正则)
#   threading    (多线程)
#   subprocess   (调用 mstsc)
#   datetime     (时间)
#   webbrowser   (打开浏览器)
#
# 以下为运行时依赖（源码运行必需；打包版已内置，无需目标机安装）：
# ============================================

# 图像处理（GUI 图标、Font Awesome 图标渲染）
Pillow==12.1.0

# 运维智脑（F17）AI 聊天：大模型 API 请求（流式 SSE 解析）
requests==2.32.5

# 以下为 FAQ 知识库（F16）新增的运行时依赖：
# ============================================

# FAQ Markdown 渲染（Markdown -> HTML，配合 tkhtmlview 展示）
markdown==3.5.1
# FAQ 富文本渲染组件（tkinter + HTML 渲染）
tkhtmlview==0.0.9

# 以下为服务器密码加密存储（S1）的可选回退依赖：
#   Windows 下默认使用系统 DPAPI（ctypes 调用 crypt32.dll），无需任何第三方库；
#   仅在非 Windows 或 DPAPI 不可用时才回退到 cryptography（Fernet）。
#   仅面向 Windows 部署时可省略，以减小发行包体积。
# ============================================

cryptography==46.0.3

# 以下为开发与打包阶段可选工具，非运行必需：
# ============================================

# 打包为 Windows 可执行文件（可选）
pyinstaller==6.3.0

# 代码格式化（可选，开发阶段）
# black==23.12.1
# flake8==7.0.0
